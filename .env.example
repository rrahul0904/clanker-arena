PORT=3000
HOST=0.0.0.0
GENERATOR_MODE=mock
JUDGE_MODE=local
DATA_FILE=.data/state.json

# Production generation (required only when GENERATOR_MODE=anthropic)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=

# Production sandbox (required only when JUDGE_MODE=judge0)
JUDGE0_URL=
JUDGE0_TOKEN=
JUDGE0_LANGUAGE_ID=71

# Unsafe override; leave disabled. Only intended for isolated developer experiments.
ALLOW_UNSAFE_REMOTE_CODE_LOCALLY=0
